# Research / experiment dependencies for the FinTrack production-upgrade sprint.
# Kept separate from the app's requirements.txt (Flask/pymysql/werkzeug) so the
# runtime service stays lean; this file makes the bake-off scripts under
# results/ and data/eval/ reproducible from a clean clone.
#
#   python -m venv .venv && .venv/Scripts/activate
#   pip install -r requirements-experiments.txt

# --- core data / ML stack (Days 1-4) ---
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
matplotlib==3.11.0
pillow==12.2.0

# --- Day 4: anomaly detection + cash-flow forecasting ---
statsmodels==0.14.6      # STL residual anomaly detection
lightgbm==4.6.0          # gradient-boosted cash-flow forecaster
prophet==1.3.0           # seasonal cash-flow forecaster

# --- Days 2-3: receipt extraction + expense categorization (NLP) ---
# Heavy; only needed to re-run run_day2_extraction.py / run_day3_categorize.py.
# Unpinned floors — install a CPU torch build to keep the download small.
transformers>=4.44
torch>=2.2
sentence-transformers>=3.0
